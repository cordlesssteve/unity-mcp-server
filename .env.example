# Conversation Search MCP Server Environment Configuration
#
# Copy this file to .env and configure with your actual values
# NEVER commit .env files with actual API keys to version control

# ============================================================================
# OpenAI Configuration (Optional - for Vector Search Features)
# ============================================================================
# Required for semantic search, vector embeddings, and AI-powered features
# Get your API key from: https://platform.openai.com/account/api-keys
# If not provided, traditional keyword search will still work
OPENAI_API_KEY=your_openai_api_key_here

# ============================================================================
# Database Configuration (Optional)
# ============================================================================
# Path to conversation database (defaults to ~/Sync/conversations.db)
# CONVERSATION_DB_PATH=/custom/path/to/conversations.db

# ============================================================================
# Server Configuration (Optional)
# ============================================================================
# Log level for debugging (debug, info, warn, error)
# LOG_LEVEL=info

# ============================================================================
# Security Best Practices
# ============================================================================
# 1. Never commit this file with actual values to version control
# 2. Use strong, unique API keys
# 3. Rotate API keys regularly
# 4. Monitor API usage and costs
# 5. Use read-only API keys when possible
# 6. Consider using a secrets management service in production